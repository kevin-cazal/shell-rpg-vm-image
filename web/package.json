{
  "name": "alpine-game-v86",
  "private": true,
  "type": "module",
  "description": "Browser runner for the Shell RPG Alpine VM (v86 + xterm serial console)",
  "scripts": {
    "prepare": "./scripts/prepare-assets.sh",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "smoke-check": "./scripts/smoke-check.sh"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "v86": "^0.5.357"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
